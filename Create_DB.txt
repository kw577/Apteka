CREATE DATABASE apteka;
USE apteka;

CREATE TABLE `category` (
	`id`			INT UNSIGNED NOT NULL AUTO_INCREMENT,
	`name`			VARCHAR(50) NOT NULL,
	`active` 		BOOLEAN,
	`navbar` 		BOOLEAN,
	PRIMARY KEY (`id`)
) ENGINE=InnoDB AUTO_INCREMENT=1 DEFAULT CHARSET=utf8;


CREATE TABLE `product` (
	`id` 			INT UNSIGNED NOT NULL AUTO_INCREMENT,
	`name`			VARCHAR(50) NOT NULL,
	`description`		VARCHAR(255),
	`price` 		DECIMAL(10,2),
	`quantity` 		INT UNSIGNED,
	`active` 		BOOLEAN,
	`category_id` 		INT UNSIGNED NOT NULL,
	`discount` 		INT UNSIGNED,
	`image`			VARCHAR(50) NOT NULL,

	PRIMARY KEY (`id`),

    FOREIGN KEY (category_id)
		REFERENCES category(id)
		ON UPDATE CASCADE ON DELETE RESTRICT

) ENGINE=InnoDB AUTO_INCREMENT=1 DEFAULT CHARSET=utf8;	



INSERT INTO product (name, description, price, quantity, active, category_id, discount, image) VALUES ('Allegra', 'Allegra to lek, który działa przeciwalergicznie i łagodzi objawy alergii takie jak katar sienny, pokrzywka, kichanie i swędzenie nosa.', 10.50, 220, true, 1, 0, produkt_1);